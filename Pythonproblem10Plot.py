# This program will display a plot of the functions x, x**x and 2**x

# I will have to import a module for this, but I have to figure out which one.


for x in range (1,4):
    a = x**x
    b = 2**x
    print (a, b, x) #this prints the results. I have to figure out now how to display a plot.